{
  "name": "trello-discord-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/trello-webhook.js && node --check api/cron-reminders.js && node --check scripts/register-webhook.js"
  }
}
